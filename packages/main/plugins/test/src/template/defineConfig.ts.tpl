import type { CommonConfig, DiffConfig } from '{{{dir}}}/interface';

export const defineCommonRuntimeConfig = (commonConfig: CommonConfig)=>{
  return commonConfig;
}

export const defineDiffRuntimeConfig = (diffConfig: DiffConfig)=>{
  return diffConfig;
}
